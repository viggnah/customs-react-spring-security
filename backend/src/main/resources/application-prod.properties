# Production Profile Configuration

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tra_customs}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USER:tra_user}
spring.datasource.password=${DB_PASSWORD:tra_password}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# H2 Console disabled in production
spring.h2.console.enabled=false

# Logging
logging.level.com.tra.customs=WARN
logging.level.org.springframework.security=WARN
